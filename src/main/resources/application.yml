server:
  port: 8083

car:
  totalCount: 100

spring:
 data:
   mongodb:
     uri: mongodb+srv://username:password@arkcluster.rxvlt.mongodb.net/BookStore

management:
  endpoint:
    health:
      group:
        custom:
          showComponents: always
          showDetails: always